apiVersion: v1
kind: Service
metadata:
    name: sattwikroytransactionmicroservice
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sattwikroytransactionmicroservice